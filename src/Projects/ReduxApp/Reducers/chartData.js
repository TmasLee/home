var defaultState = {
  
}

function chartData(state=defaultState, action){
  switch(action.type){
    case(''):
      return {

      }
    
      default:
        return state;
  }
}

export default chartData;